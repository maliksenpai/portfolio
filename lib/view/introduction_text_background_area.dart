import 'package:flutter/material.dart';

class IntroductionTextBackgroundArea extends StatelessWidget {
  const IntroductionTextBackgroundArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
